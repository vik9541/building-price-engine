# 📈 Ценовая Стратегия

## Обзор

Основная таска движка - автоматически рассчитывать **оптимальные цены** для максимизации конкурентоспособности и прибыли.

## Ключевые Параметры

### 1. Минимальная Наценка (min_markup)
**Дефалт:** 10%

Гарантирует, что каждый товар финансируется.

```
min_price = cost * (1 + min_markup/100)
```

### 2. Максимальная Наценка (max_markup)
**Дефалт:** 30%

Ограничивает максимальные цены для ссертива.

```
max_price = cost * (1 + max_markup/100)
```

### 3. Минимум Маржа (min_margin_rub)
**Дефалт:** 50 рублей

Гарантирует вассолютные минимальные прибыли.

```
if (final_price - cost) < min_margin_rub:
    final_price = cost + min_margin_rub
```

### 4. Вес Конкурентов (competitor_weight)
**Дефалт:** 0.7 (70%)

Насколько сильно мы дожим за конкурентами.

### 5. Вес Популярности (popularity_weight)
**Дефалт:** 0.2 (20%)

Как сильно популярные товары влияют на цену.

### 6. Процент Недоценки (undercut_percentage)
**Дефалт:** 5%
Мы делаем цены на это процент ниже конкурентов.

## Алгоритм рассчёта

### Шаг 1: Базовая цена

```python
base_price = cost * (1 + min_markup / 100)
```

Пример: cost=100рюб, min_markup=10%
```
base_price = 100 * 1.10 = 110рюб
```

### Шаг 2: Анализ цен конкурентов

```python
competitor_prices = [120, 125, 118]  # Цены конкурентов
avg_competitor_price = sum(prices) / len(prices)  # 121
min_competitor_price = 118
max_competitor_price = 125
```

### Шаг 3: Сравнительный анализ

```python
# Конкуренты оно чым на 10%+ дороже?
if avg_competitor_price > base_price * 1.15:
    # Да, мы можем повысить цену
    final_price = avg_competitor_price * 0.95  # Кусаем на 5%

# Конкуренты на 15%+ дешевле?
elif avg_competitor_price < base_price * 0.85:
    # Да, нужно снизить цену
    final_price = avg_competitor_price * 1.02  # Небольшое премиум

else:
    # Конкуренты рядом
    final_price = max(base_price, avg_competitor_price * 0.98)
```

### Шаг 4: Контроль минимальной прибыли

```python
min_price = cost + min_margin_rub  # 100 + 50 = 150
if final_price < min_price:
    final_price = min_price
```

### Шаг 5: Контроль максимального наценка

```python
max_price = cost * (1 + max_markup / 100)  # 100 * 1.30 = 130
if final_price > max_price:
    final_price = max_price
```

## Примеры Рассчётов

### Сценарий 1: Присоблюдение конкурентам

```
Параметры:
- cost = 100рюб
- min_markup = 10%
- max_markup = 30%
- min_margin = 50рюб
- Конкуренты: 115, 120, 118
- avg = 117.67

После рассчёта:
base_price = 110 (100 * 1.1)
avg_competitor > base_price * 1.15 (117.67 > 126.5)? Нет
avg_competitor < base_price * 0.85 (117.67 < 93.5)? Нет
final_price = max(110, 117.67 * 0.98) = max(110, 115.32) = 115.32

Контроль раванси:
min_price = 100 + 50 = 150
final_price (115.32) < 150? Да, наставим = 150
max_price = 100 * 1.3 = 130
final_price (150) > 130? Да, наставим = 130

РЕЗУЛЬТАТ: 130рюб
```

### Сценарий 2: Ниже конкурентов

```
Параметры:
- cost = 100рюб
- Конкуренты: 80, 85, 82
- avg = 82.33

После рассчёта:
base_price = 110
avg_competitor < base_price * 0.85 (82.33 < 93.5)? Да!
final_price = avg_competitor * 1.02 = 82.33 * 1.02 = 83.98

Контроль:
min_price = 100 + 50 = 150
final_price (83.98) < 150? Да, наставим = 150

РЕЗУЛЬТАТ: 150рюб
```

## ОПТИМИЗАЦИОННЫЕ КОНФИГУРАЦИИ

### Агрессивная стратегия (борются за объём)
```
min_markup: 5%
max_markup: 20%
competitor_weight: 0.85
min_margin: 30
```

### Напористая стратегия (борются за прибыль)
```
min_markup: 20%
max_markup: 50%
competitor_weight: 0.4
min_margin: 200
```

## Мониторинг

Система автоматически логирует:
- Все новые цены
- Мотивы изменений
- даты исменений
- Какая стратегия была использована
